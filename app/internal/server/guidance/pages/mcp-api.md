Agents and other clients can inspect and modify Universal Curriculum through MCP and the HTTP API. Both use the same application workflows and domain rules as the web interface.

Use MCP resources to inspect documentation and published curriculum state. Use the relevant tools or API operations for authenticated changes; curriculum changes are prepared in proposals before publication.
