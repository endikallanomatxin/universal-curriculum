Write concise, clear educational content that teaches the unit's concept directly. Include enough explanation to make the unit useful, and remove material that does not serve that concept.

Content supports Markdown. LaTeX is supported inside Markdown: use `$...$` for inline math and `$$...$$` for display math.
